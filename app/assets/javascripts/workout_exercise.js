function WorkoutExercise(workout_exercise) {
    this.id = workout_exercise.id;
    this.sets = workout_exercise.sets;
    this.repetitions = workout_exercise.repetitions;
    this.exercise_name = workout_exercise.exercise_name;
}